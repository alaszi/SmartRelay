# SmartRelay Environment Configuration
# Copy this file to .env and fill in real values (never commit .env!)

# === Telegram Notifications ===


# === Database (future use) ===
DB_HOST=localhost
DB_NAME=smartrelay
DB_USER=smartrelay
DB_PASS=

# === App ===
APP_ENV=production   # production | development
APP_DEBUG=false
